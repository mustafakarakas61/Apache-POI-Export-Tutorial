server:
  port: 8080

spring:
  datasource:
    username: postgres
    password: 180200
    url: jdbc:postgresql://localhost:5432/apache-poi-tutorial
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  data:
    mongodb:
      host: localhost
      port: 27017
      database: export-tutorial